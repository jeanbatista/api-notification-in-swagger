const SolicitacaoModel = function () { };

SolicitacaoModel.prototype.solicitarPedido = async function(obj, callback) {
    callback(obj);
}

SolicitacaoModel.prototype.enviarOrcamento = async function(obj, callback) {
    callback(obj);
}

SolicitacaoModel.prototype.aprovarPedido = async function(obj, callback) {
    callback(obj);
}

SolicitacaoModel.prototype.reprovarPedido = async function(obj, callback) {
    callback(obj);
}

SolicitacaoModel.prototype.verificarSituacaoPedido = async function(obj, callback) {
    callback(obj);
}

module.exports = SolicitacaoModel;